$(document).ready(function() {
   /*var $window = $(window);

    // Function to handle changes to style classes based on window width
    function checkWidth() {
        if ($window.width() < 980) {
            $('#button-comanda').removeClass('btn-lg').addClass('btn-small');
            $('#button-adicionar').removeClass('btn-lg').addClass('btn-small');
        };

        if ($window.width() >= 980) {
            $('#button-comanda').removeClass('btn-small').addClass('btn-lg');
            $('#button-adicionar').removeClass('btn-small').addClass('btn-lg');
        }
    }

    // Execute on load
    checkWidth();

    // Bind event listener
        $(window).resize(checkWidth);*/
});